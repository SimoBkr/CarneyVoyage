server:
  port: 9095


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: admin
    url: jdbc:mysql://localhost:3306/productservice?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
  jpa:
    show-sql: true
    hibernate:
        ddl-auto: create
    properties:
        hibernate:
            dialect: org.hibernate.dialect.MySQL5Dialect

